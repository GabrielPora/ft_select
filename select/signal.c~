#include "ft_select.h"

void sig_stop(int signo)
{
  if (signo == SIGSTOP)
    {
      tmp->sig = 0;
      while (tmp->sig == 0)
	sleep(1);
    }
}

void sig_cont(int signo)
{
  if (signo == SIGCONT)
    {
      tmp->sig = 1;
    }
}
